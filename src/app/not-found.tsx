"use client";
import { ErrorComponent } from "./_components";

const ListingErrorPage = () => {
    return <ErrorComponent title="Page not found!" subTitle="But do not worry, you can always go back to our home page and start over." />;
};

export default ListingErrorPage;
